# Young’s Physics v2.5.0 패치 적용 방법

이 패치는 다음 세 가지를 함께 수정합니다.

1. **이전 회차를 나중에 입력해도 기존 상위 회차 성적표가 최신화**됩니다.
2. **Google Sheets 저장 키를 제출 때마다 다시 묻지 않습니다.** 교사용 기기에서 한 번만 입력합니다.
3. **엑셀·Google Sheets 탭 붙여넣기의 빈 셀을 미기입으로 보존**합니다.

---

## 1. GitHub Pages 파일 교체

이 패치 폴더 안의 파일을 기존 GitHub 저장소의 **동일한 경로**에 덮어씁니다.

```text
README.md
package.json
site/index.html
site/report.html
site/guide.html
site/assets/config.js
site/assets/core.js
site/assets/app.js
site/assets/report.js
site/assets/styles.css
```

GitHub 웹 화면에서는 파일별로 같은 경로에 업로드하거나 수정한 뒤 커밋합니다. 파일 수가 100개보다 적으므로 한 번에 적용할 수 있습니다.

커밋 후 **Actions**에서 GitHub Pages 배포가 끝날 때까지 기다립니다. 배포 후 브라우저에서 강력 새로고침합니다.

```text
Windows: Ctrl + Shift + R
Mac: Cmd + Shift + R
```

---

## 2. Apps Script `Code.gs` 교체 및 재배포 — 필수

GitHub 파일만 교체하면 이전 회차 동적 재계산이 서버에 적용되지 않습니다. Apps Script도 반드시 갱신해야 합니다.

1. 성적 DB Google Sheet를 엽니다.
2. **확장 프로그램 → Apps Script**를 엽니다.
3. 기존 `Code.gs` 내용을 전부 지웁니다.
4. 패치의 `apps-script/Code.gs` 전체를 붙여넣습니다.
5. 저장합니다.
6. **배포 → 배포 관리**를 엽니다.
7. 현재 웹 앱의 연필 모양 **편집**을 누릅니다.
8. 버전을 **새 버전**으로 선택합니다.
9. **배포**를 누릅니다.

기존 `/exec` 주소는 그대로 사용할 수 있습니다.

```text
https://script.google.com/macros/s/AKfycbx3PkumTW3dFg1nvXpgcp3o0Y3NX7YfrcMyDUtYB_uMVAKsxe6SDPWi0clMBR-SPwkwzA/exec
```

---

## 3. Google Sheets 저장 키는 한 번만 입력

배포된 교사용 사이트를 연 뒤 **누적 저장 방식 설정**을 펼칩니다.

1. 저장 방식이 `Google Sheets + Apps Script`인지 확인합니다.
2. **Google Sheets 저장 키** 칸에 Apps Script의 `WRITE_KEY` 값을 한 번 입력합니다.
3. 입력 칸을 벗어나면 해당 교사 기기의 브라우저에 저장됩니다.

이후에는 학생을 제출할 때 저장 키 팝업이 다시 나타나지 않습니다.

저장 키는 GitHub 코드나 학생 링크에 포함되지 않지만, 교사 기기의 `localStorage`에 저장됩니다. 공용 컴퓨터에서는 작업 후 **저장 키 지우기**를 누르세요.

---

## 4. 이전 회차를 나중에 입력한 경우

동일 학생 판정 기준은 다음과 같습니다.

```text
학교 + 학생 이름
```

따라서 회차마다 학교와 이름을 동일하게 입력해야 합니다.

예시:

1. 김물리 학생의 5회 결과를 먼저 저장
2. 같은 학교·이름으로 4회 결과를 나중에 저장
3. 기존 5회 학생 링크를 다시 열기
4. 서버에서 4회와 5회를 다시 조회하여 5회 분석에 4회가 포함됨

이미 성적표 탭이 열려 있다면 상단의 **최신 분석** 버튼을 누르거나, 다른 탭으로 이동했다 돌아오면 자동 갱신됩니다.

- 5회 리포트: 4회와 5회를 표시
- 6회 리포트: 등록된 1~6회 기록을 표시
- 4회 리포트: 미래 회차인 5회 이후 기록은 표시하지 않음

### 기존 링크 처리

- `#id=...` 형태: 바로 동적 최신화
- 예전 `#report=...` 링크 안에 서버 토큰이 남아 있는 경우: 열 때 자동으로 `#id=...` 방식으로 전환
- 서버 토큰이 없는 순수 브라우저 백업 링크: 교사용 기록 목록에서 **링크 복사**를 한 번 눌러 새 서버 링크를 발급

---

## 5. 엑셀 답안 붙여넣기

다음처럼 엑셀 한 행을 그대로 복사해 붙여넣을 수 있습니다.

```text
1		4	3	1	2	3	5	2	2	5	5	1	2	4	1	3	5	5	
```

적용 결과:

```text
1번 = 1
2번 = 미기입
3번 = 4
...
19번 = 5
20번 = 미기입
```

빈 셀 때문에 뒤 답안이 앞으로 당겨지지 않습니다. 2번과 20번은 **미기입 0점**이며 오답 감점 `-1.25점`이 적용되지 않습니다.

엑셀·Google Sheets에서는 답안 한 행 전체를 선택해 복사한 뒤 **답안 한 번에 붙여넣기 → 붙여넣기 적용**을 사용하세요.

---

## 6. 확인 방법

패치 적용 후 다음을 확인합니다.

- 학생 제출 시 저장 키 팝업이 나타나지 않음
- 설정 영역의 저장 키를 한 번 입력한 뒤 새로고침해도 유지됨
- 5회 먼저 저장 → 4회 나중 저장 → 기존 5회 링크의 이력에 4회 포함
- 4회 링크에는 5회가 표시되지 않음
- 탭 붙여넣기의 두 번째 빈 셀이 2번 미기입으로 표시됨
- 끝의 빈 셀이 20번 미기입으로 표시됨
- 미기입 문항 배점이 0점으로 표시됨
