# v2.5.0 검증 결과

## 통과한 검사

- 사용자 제공 탭 문자열을 20개 문항 위치로 해석
- 2번 빈 셀을 미기입으로 보존
- 20번 끝 빈 셀을 미기입으로 보존
- 미기입 문항 배점 0점 확인
- 공백 구분의 `-`, `0`, `X` 미기입 처리 유지
- 쉼표 구분의 연속 빈 셀 보존
- 5회 먼저 입력 후 4회 추가 시 5회 이력 `[4, 5]` 확인
- 4회 리포트에서 미래 5·6회 제외 확인
- Apps Script 서버 이력 계산에서도 동일 결과 확인
- `window.prompt()` 제거 확인
- WRITE_KEY의 교사 기기 `localStorage` 저장 확인
- 서버 저장 실패 시 오래된 정적 링크를 대신 생성하지 않도록 차단 확인
- 기존 정적 링크의 서버 토큰 자동 전환 확인
- JavaScript 및 Apps Script 구문 검사 통과
- 전체본 `npm test` 통과
- 정적 HTTP 페이지와 v2.5.0 자산 경로 로딩 확인

## 브라우저 자동화

실행 환경의 브라우저 자동화가 시간 제한에 걸려 전체 UI 자동 클릭 검사는 완료하지 못했습니다. 핵심 채점·붙여넣기·이력 계산·구문·정적 페이지 로딩은 위 검사로 확인했습니다.
